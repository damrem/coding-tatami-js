'use strict';

describe('Unit testing the app', function(){

	beforeEach(function(){
		//setup
	});

	afterEach(function(){
		//cleanup
	});

	it('should add 2 numbers', function(){
		expect(myModule.add(1, 2)).toBe(3);
	});
});